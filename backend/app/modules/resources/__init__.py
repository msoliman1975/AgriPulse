"""Resources module — flat catalog of workers + equipment per farm.

Resources are assigned to ``plan_activities`` via the
``activity_resources`` join table. See board PR-2 in
[[project_board_revamp]].
"""
