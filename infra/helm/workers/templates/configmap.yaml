apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "missionagre-workers.fullname" . }}
  labels:
    {{- include "missionagre-workers.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
