apiVersion: v2
name: missionagre-workers
description: |
  MissionAgre Celery workers (light + heavy queues) and beat scheduler.
  ARCHITECTURE.md § 3.1 commits Celery + Redis broker; this chart runs
  the three matching Deployments off the same image as the API.
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.28.0"
maintainers:
  - name: msoliman1975
keywords:
  - missionagre
  - celery
  - workers
