apiVersion: v2
name: agripulse-workers
description: |
  AgriPulse Celery TASK workers (light + heavy task queues) and beat
  scheduler. NOTE: "task workers" here are background-job processes — NOT
  the in-app "Workers" (farm personnel under Settings ▸ Workers, the
  resources module). ARCHITECTURE.md Â§ 3.1 commits Celery + Redis broker;
  this chart runs the three matching Deployments off the same image as the API.
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.28.0"
maintainers:
  - name: msoliman1975
keywords:
  - agripulse
  - celery
  - workers
