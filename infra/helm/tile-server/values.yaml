image:
  repository: ghcr.io/msoliman1975/missionagre/tile-server
  tag: ""
  pullPolicy: IfNotPresent
imagePullSecrets: []

replicaCount: 2
revisionHistoryLimit: 5

serviceAccount:
  create: true
  name: ""
  # IRSA: the role ARN is set per-env in the ApplicationSet overlay so
  # tile-server pods can read COGs from the imagery bucket directly.
  annotations: {}

service:
  type: ClusterIP
  port: 8000

ingress:
  enabled: true
  className: nginx
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    cert-manager.io/cluster-issuer: letsencrypt-staging
  host: tiles.dev.missionagre.local
  pathPrefix: /
  tlsSecretName: missionagre-tile-server-tls

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: "2"
    memory: 2Gi

podAnnotations: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  fsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

containerSecurityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop: [ALL]

probes:
  liveness:
    httpGet:
      path: /healthz
      port: http
    initialDelaySeconds: 10
    periodSeconds: 30
    timeoutSeconds: 5
  readiness:
    httpGet:
      path: /healthz
      port: http
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 5

env:
  AWS_DEFAULT_REGION: me-south-1
  CORS_ALLOW_ORIGINS: "https://app.dev.missionagre.local"
  DEFAULT_MAX_ZOOM: "22"
  DEFAULT_MIN_ZOOM: "0"
  GDAL_HTTP_MERGE_CONSECUTIVE_RANGES: "YES"

serviceMonitor:
  enabled: true
  interval: 30s
  scrapeTimeout: 10s

hpa:
  enabled: false
  minReplicas: 2
  maxReplicas: 8
  targetCPUUtilizationPercentage: 80

pdb:
  enabled: true
  minAvailable: 1

nodeSelector: {}
tolerations: []
affinity: {}
