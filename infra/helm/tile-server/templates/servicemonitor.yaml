{{- if .Values.serviceMonitor.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "agripulse-tile-server.fullname" . }}
  labels:
    {{- include "agripulse-tile-server.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "agripulse-tile-server.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: http
      path: /metrics
      interval: {{ .Values.serviceMonitor.interval }}
      scrapeTimeout: {{ .Values.serviceMonitor.scrapeTimeout }}
{{- end }}
