apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agripulse-tile-server.fullname" . }}
  labels:
    {{- include "agripulse-tile-server.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ tpl ($v | toString) $ | quote }}
  {{- end }}
