apiVersion: v1
kind: Service
metadata:
  name: {{ include "missionagre-tile-server.fullname" . }}
  labels:
    {{- include "missionagre-tile-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "missionagre-tile-server.selectorLabels" . | nindent 4 }}
