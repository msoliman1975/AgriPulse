apiVersion: v1
kind: Service
metadata:
  name: {{ include "agripulse-tile-server.fullname" . }}
  labels:
    {{- include "agripulse-tile-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "agripulse-tile-server.selectorLabels" . | nindent 4 }}
