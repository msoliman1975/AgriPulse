apiVersion: v2
name: missionagre-tile-server
description: |
  TiTiler-based COG → XYZ/WMTS service. ARCHITECTURE.md § 9 commits this
  as the only tile layer for MissionAgre; the chart deploys our wrapped
  image with COG storage routed via IRSA-attached IAM credentials.
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.28.0"
maintainers:
  - name: msoliman1975
keywords:
  - missionagre
  - tiles
  - titiler
  - cog
