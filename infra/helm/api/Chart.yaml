apiVersion: v2
name: agripulse-api
description: |
  AgriPulse backend API (FastAPI). Multi-tenant farm-management service
  per ARCHITECTURE.md Â§ 3.1; runs as a stateless Deployment behind the
  NGINX ingress, with Prometheus metrics scraped on a separate port.
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.28.0"
maintainers:
  - name: msoliman1975
keywords:
  - agripulse
  - api
  - fastapi
