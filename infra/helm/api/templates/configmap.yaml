apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "missionagre-api.fullname" . }}
  labels:
    {{- include "missionagre-api.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
