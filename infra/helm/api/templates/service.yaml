apiVersion: v1
kind: Service
metadata:
  name: {{ include "missionagre-api.fullname" . }}
  labels:
    {{- include "missionagre-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
    - name: metrics
      port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "missionagre-api.selectorLabels" . | nindent 4 }}
