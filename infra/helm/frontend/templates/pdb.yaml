{{- if .Values.pdb.enabled -}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "missionagre-frontend.fullname" . }}
  labels:
    {{- include "missionagre-frontend.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "missionagre-frontend.selectorLabels" . | nindent 6 }}
{{- end }}
