apiVersion: v1
kind: Service
metadata:
  name: {{ include "missionagre-frontend.fullname" . }}
  labels:
    {{- include "missionagre-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "missionagre-frontend.selectorLabels" . | nindent 4 }}
