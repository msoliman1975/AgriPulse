apiVersion: v1
kind: Service
metadata:
  name: {{ include "agripulse-frontend.fullname" . }}
  labels:
    {{- include "agripulse-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "agripulse-frontend.selectorLabels" . | nindent 4 }}
