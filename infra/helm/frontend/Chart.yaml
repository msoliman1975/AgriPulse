apiVersion: v2
name: missionagre-frontend
description: |
  MissionAgre frontend SPA, served by nginx-unprivileged after a Vite build.
  Stateless, ingress-fronted, no secrets at runtime — environment-specific
  values are injected at build time via `pnpm build` arguments.
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.28.0"
maintainers:
  - name: msoliman1975
keywords:
  - missionagre
  - frontend
  - spa
