# Copy to .sim.env and fill in. Gitignored.
SIM_API_BASE=https://api.agripulse.cloud/api/v1
SIM_KC_BASE=https://keycloak.agripulse.cloud
SIM_REALM=agripulse
SIM_KC_CLIENT=agripulse-api
SIM_EMAIL_DOMAIN=agripulse.tech

# MUST equal the deployed NOTIFICATION_SINK_TENANT_PREFIX, or this run mails
# real people. Act 3 asserts on the dispatch rows to prove it did not.
SIM_TENANT_PREFIX=sim-

SIM_PLATFORM_USER=
SIM_PLATFORM_PASS=
SIM_KC_ADMIN_USER=
SIM_KC_ADMIN_PASS=
