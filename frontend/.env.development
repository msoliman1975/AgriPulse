# Dev defaults — committed on purpose so `pnpm dev` works out of the box
# against infra/dev/compose.yaml. Personal overrides go in .env.local
# (gitignored). Production builds get values injected at deploy time.

VITE_API_BASE_URL=/api

VITE_OIDC_AUTHORITY=http://localhost:8080/realms/missionagre
VITE_OIDC_CLIENT_ID=missionagre-api
VITE_OIDC_REDIRECT_URI=http://localhost:5173/auth/callback
VITE_OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:5173/
VITE_OIDC_SCOPE=openid profile email
